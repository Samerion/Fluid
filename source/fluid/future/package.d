/// This package holds modules originally added to 0.8.0 that were ported back to 0.7.x. Because 0.8.0 uses a new 
/// package structure, these make little sense in 0.7.x structure, and so, are all placed in this directory.
///
/// These modules are experimental and prone to breakage.
///
/// This module has no public imports: All future packages are meant to be imported individually.
module fluid.future;
